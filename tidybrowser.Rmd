---
title: "Untitled"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
options(usethis.full_name = "Benjamin Guinaudeau")
library(pkgverse)

tidybrowse <- c("RSelenium", "dockeR", "tidyselenium", "tidyweb", "selinput", "bashR", "hideR", "rvest")

## create packages dir
# dir.create("../tidybrowser")

## create tidyweb pkgverse
pkgverse(pkg = "tidybrowse",
         pkgs = tidybrowse,
         keep = "..",
         # use = c("readme_rmd", "rstudio", "mit_license", "git"),
         install_if = TRUE
)

library(tidybrowse)

```

